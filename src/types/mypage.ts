export type MyInfo = { 
    store: number;
    production: number;
    upload: number;
};

export type MyPageState = {
    store: number;
    production: number;
    upload: number;
    loading: boolean;
    error: any
};
