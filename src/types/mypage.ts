export type MyInfo = { 
    store: number;
    production: number;
    uploadSales: number;
};

export type MyPageState = {
    store: number;
    production: number;
    uploadSales: number;
    loading: boolean;
    error: any
};
