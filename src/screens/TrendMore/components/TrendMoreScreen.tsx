import React from 'react';
import styled from 'styled-components/native';
import { TrendMoreProps } from '@base/types/Navigation/TrendSignatureNavigation';

type TrendMoreScreenProps = {
  navigation: TrendMoreProps['navigation'];
};

export default function TrendMoreScreen({ navigation }: TrendMoreScreenProps) {
  return null;
}
